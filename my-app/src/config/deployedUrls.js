/**
 * Production URLs for this deployment (used only when env vars are missing at build time).
 * Prefer setting VITE_API_URL on Vercel and ALLOWED_ORIGINS on Render — these are fallbacks.
 */
export const MINDSPACE_RENDER_API_ORIGIN = "https://mindspace-j7br.onrender.com";
export const MINDSPACE_VERCEL_APP_ORIGIN = "https://mindspace-eight-ruddy.vercel.app";
